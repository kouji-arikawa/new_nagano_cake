<h2>商品詳細</h2>

<%= image_tag @item.image, size: "200x200" %>
<br>
<p>商品名<%= @item.name %></p>
<p>商品説明<%= @item.introduction %></p>
<p>ジャンル<%= @item.genre.name %></p>
<p>
    税込価格<%= @item.add_tax_price.to_s(:dalimited) %><br>
    税抜価格<%= @item.price %>
</p>
<P>
    販売ステータス
    <% if @item.is_active %>
    販売中
    <% else %>
    販売停止中
    <% end %>
</P>

<%= link_to "編集する", edit_admin_item_path(@item.id) %>